# OFFICE — simple (Node latest OK)

Ab Visual Studio / Node 20 force nahi.

## Steps
1. Node.js **latest** install: https://nodejs.org (22 ya 24 OK)
2. Folder: `C:\Pepsi` (OneDrive se behtar)
3. Clone:
   ```text
   cd C:\
   git clone https://github.com/abbasinkhan567-beep/DMS-BY-AK-HK-AK.git Pepsi
   cd C:\Pepsi
   git pull
   ```
4. **`FIX-OFFICE.bat`** double-click
5. Login: `admin123`
6. Settings → Sync → token → Sync Now

Database ab Node built-in SQLite use karti hai — `better-sqlite3` nahi, isliye compile error nahi aayega.

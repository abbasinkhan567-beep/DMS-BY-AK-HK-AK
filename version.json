{
  "version": "10.2.1",
  "name": "Pepsi Distribution",
  "notes": "v10.2.1: Updated the desktop launcher (start-server.ps1) to install with npm install --include=dev so TypeScript types are never pruned on npm 12, and it now prints the last lines of server-log.txt when a build fails so problems are visible immediately.",
  "updatedAt": "2026-08-01"
}

{
  "version": "10.2.0",
  "name": "Pepsi Distribution",
  "notes": "v10.2.0: Fixed the update failing on newer npm (12+) — Next.js tried to auto-install TypeScript types with a flag npm now rejects, so the build aborted. The update now installs the type packages itself and allows install scripts (esbuild/sharp) via package.json so Apply Updates succeeds on every PC.",
  "updatedAt": "2026-08-01"
}
